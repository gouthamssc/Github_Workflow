name: Multiple Variable Workflow

on:
  workflow_dispatch:
jobs:
  hello_world:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Print a message
        run: |
          echo "${{ secrets.key }}"
