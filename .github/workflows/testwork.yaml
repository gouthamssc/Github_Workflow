name: JSON POST Workflow

on:
  workflow_dispatch:
    inputs:
      json_payload:
        description: 'JSON payload'
        required: true

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v2
      - name: Print JSON payload
        run: |
          echo "Received JSON Payload: ${{ (github.event.inputs.json_payload).var_serviceNowRequest) }}"    
#       - name: Print ServiceNow Request Data
#         run: echo "ServiceNow Request Data: ${{ toJSON(fromJSON(github.event.inputs.json_payload).var_serviceNowRequest) }}"
